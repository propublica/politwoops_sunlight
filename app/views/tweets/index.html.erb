<p class="intro">
  <strong>Politwoops is back! And we’re working to update and expand features that will make it easier to filter and find deleted tweets. Let us know what features you’d like to see by emailing us <a href="mailto:twoops@sunlightfoundation.com">here</a>.</strong>
</p>

<div class="title">
	<h2>
		Recent Politwoops
	</h2>

  <%= render :template => "shared/filterform" %>

</div>
<div class="caveat"><p>This may be an incomplete list. If you think we're missing someone, please <a href="mailto:twoops@sunlightfoundation.com">email us</a> with their Name, State, Political Party, Office they hold or are seeking and, of course, Twitter handle. Thanks!</p>
</div>

<% content_for :feeds do %>
  <% if @politician %>
    <%= auto_discovery_link_tag :rss, politician_url(@politician.user_name, :format => :rss) %>
  <% elsif @query %>
    <%= auto_discovery_link_tag :rss, index_url(:format => :rss, :q => @query) %>
  <% else %>
    <%= auto_discovery_link_tag :rss, index_url(:format => :rss) %>
  <% end %>
<% end %>

<% if @tweets.empty? %>
  No results for the filter options you selected.
<% else %>
  <%= render :partial => "tweets/tweet", :collection => @tweets, :object => :tweet %>

  <%= render :template => "shared/pager" %>

  <%= render :template => "shared/screenshotbox" %>
<% end %>
